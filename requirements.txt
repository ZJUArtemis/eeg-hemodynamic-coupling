# Python >= 3.10. Versions used in the study (loosely pinned).
numpy>=1.24
scipy>=1.11
pandas>=2.0
statsmodels>=0.14
PyWavelets>=1.4
scikit-learn>=1.3
matplotlib>=3.7
# VitalDB data access (only needed to regenerate features from raw waveforms)
vitaldb>=1.4
